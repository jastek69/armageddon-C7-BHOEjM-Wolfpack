lab2a-alb01-783038921.us-east-2.elb.amazonaws.com
curl -I https://lab2a-alb01-783038921.us-east-2.elb.amazonaws.com


curl -I https://aster-project.site
  curl -I https://app.aster-project.site

Verification CLI (students must prove all 3 requirements)
1) “VPC is only reachable via CloudFront”
A) Direct ALB access should fail (403)
  curl -I https://lab2a-alb01-783038921.us-east-2.elb.amazonaws.com

Expected: 403 (blocked by missing header)

B) CloudFront access should succeed
  curl -I https://aster-project.site
  curl -I https://app.aster-project.site

Expected: 200/301 → 200

2) WAF moved to CloudFront
  aws wafv2 get-web-acl \
  --name lab2a-cf-waf01 \
  --scope CLOUDFRONT \
  --id 97d71a48-452c-4661-b28b-92b495c2c6a5

And confirm distribution references it:
  aws cloudfront get-distribution \
  --id E2I43XW8O3TESS \
  --query "Distribution.DistributionConfig.WebACLId"

Expected: WebACL ARN present.

3) aster-poject.site points to CloudFront
  dig aster-poject.site A +short
  dig app.aster-project.site A +short

Expected: resolves to CloudFront (you’ll see CloudFront anycast behavior, not ALB IPs)



aws cloudfront get-distribution \
  --id E1QOZEHG99EMC0 \
  --query "Distribution.DistributionConfig.WebACLId" \
  --output text