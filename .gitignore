.DS_Store
**/.DS_Store
*.pem

# Local .terraform directories
**/.terraform/*

# Secret environment file (contains PSKs and db_password — never commit)
.secrets.env


# Terraform files are ignored for this lab. If you want to commit Terraform code later,
# comment out the next line.
# *.tf

# Ignore all .tfvars files, which are likely to contain sensitive data, such as
# passwords, private keys, and other secrets. These should not be part of version
*.tfvars


# .tfstate files
*.tfstate
*.tfstate.*
state-*.json
state-*.backup



# Crash log files
crash.log
crash.*.log
terraform_errors.log

# Error and debug log files
*.log
error.log
debug.log
terraform.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# passwords, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Terraform lock file
*.lock.hcl


# Terraform folder
.terraform\

tfplan*
*.tfplan
tfplan_infra
tfplan_bootstrap
plan
plan.json
plan.tfgraph
terraform-graph.png
bootstrap-graph.png
planned_resources.txt

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore keys
*.pem
*.csr

# NOTE: JSON files are currently allowed. If you want to block all JSON again,
# uncomment the next line:
# *.json

# GCP service account keys (never commit)
newyork_gcp/taaops-e9943412868a.json

# Common local artifacts (uncomment if you want them ignored)
# .DS_Store
# Thumbs.db

# Accidental CLI option files (created by broken line continuations)
--*
*.key
*.p12
lambda/alarm_hook.zip
lambda/ir_reporter.zip

# Archive directory (one-off debug scripts, not part of deployable code)
archive/

# Codex skills (local only)
.codex/
*.skill
SkillBuilder/

# Allow lab deliverables and reports to be committed
!LAB1-DELIVERABLES/
!LAB1-DELIVERABLES/**
!ir-report.md

# Allow files to override negated patterns
# Remove hash symbol to enable
# !example.json

# Python cache/build artifacts
python/__pycache__/
**/__pycache__/

# Generated translation outputs (local test artifacts)
tests/localized/
LAB4-DELIVERABLES/localized/*.json

# Generated sanity snapshots
tests/sanity_check_*.jsonl
tests/sanity_check_*.md

# archived files
./archive/
