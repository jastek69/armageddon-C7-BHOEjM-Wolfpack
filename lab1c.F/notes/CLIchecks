Student verification (CLI) A) Confirm WAF logging is enabled (authoritative) 



aws wafv2 get-logging-configuration \
--resource-arn arn:aws:wafv2:us-east-2:571938892172:regional/webacl/lab1c-waf01/f3d8e0ae-f6ed-43da-b882-937cf63f32b1

WAF_ARN=$(aws wafv2 list-web-acls \
  --scope REGIONAL \
  --region us-east-2 \
  --query "WebACLs[?Name=='lab1c-waf01'].ARN | [0]" \
  --output text)

aws wafv2 get-logging-configuration \
  --resource-arn "$WAF_ARN" \
  --region us-east-2

Expected: LogDestinationConfigs contains exactly one destination.


     

B) Generate traffic (hits + blocks) curl -I https://aster-project.site/ curl -I https://app.aster-project.site/


C2) If S3 destination aws s3 ls s3://aws-waf-logs-lab1c-571938892172/ --recursive | head



Why this makes incident response “real” Now you can answer questions like: “Are 5xx caused by attackers or backend failure?” “Do we see WAF blocks spike before ALB 5xx?” “What paths / IPs are hammering the app?” “Is it one client, one ASN, one country, or broad?” “Did WAF mitigate, or are we failing downstream?”
This is precisely why WAF logging destinations include CloudWatch Logs (fast search) and S3/Firehose (archive/SIEM pipeline)

aws route53 list-hosted-zones-by-name \
  --dns-name aster-project.site \
  --query "HostedZones[].Id"


aws route53 list-resource-record-sets \
  --hosted-zone-id Z0952693338Z8UVLRBHYT \
  --query "ResourceRecordSets[?Name=='app.aster-project.site.']"


Confirm certificate issued

aws acm list-certificates \
  --region us-east-2 \
  --query "CertificateSummaryList[].CertificateArn" \
  --output text


aws acm describe-certificate \
--region us-east-2 \
--certificate-arn arn:aws:acm:us-east-2:571938892172:certificate/f7ed90b0-c587-41cc-be25-ce7d34992795 \
--query "Certificate.Status"
